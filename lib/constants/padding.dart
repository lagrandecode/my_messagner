import 'package:flutter/material.dart';

//here are the paddings and the margins used in the app


Padding padding1 = Padding(padding: EdgeInsets.all(10.0),);
Padding padding2 = Padding(padding: EdgeInsets.all(15.0),);
 const marginLeft18 = EdgeInsets.only(left: 18.0);

